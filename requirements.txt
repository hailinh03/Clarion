# ─────────────────────────────────────────────
# Clarion — requirements.txt
# Stack: FastAPI + LangChain + Qdrant + Groq + local embedding (BAAI/bge-m3)
# ─────────────────────────────────────────────

# --- Web Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12        # for file upload (BRD)
python-dotenv==1.0.1

# --- LangChain core ---
langchain==0.3.7
langchain-core==0.3.19
langchain-community==0.3.7

# --- LLM Providers (chỉ cần Groq cho free tier) ---
langchain-groq==0.2.1
langchain-google-genai==2.0.1

# --- Embedding local (BAAI/bge-m3) ---
sentence-transformers==3.3.1
torch>=2.0.0                    # CPU version; đổi sang cu121 nếu có GPU
transformers==4.46.3
huggingface-hub==0.26.2

# --- Vector DB ---
qdrant-client==1.12.1

# --- Async Task Queue ---
aio-pika==9.4.1

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.30.0

# --- Data Validation ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Utilities ---
numpy>=1.26.0,<2.0.0
httpx==0.28.0                   # async HTTP (Jira API)
tenacity==9.0.0                 # retry logic cho LLM / embed calls
loguru==0.7.3                   # structured logging

# --- Document parsing (BRD upload) ---
pypdf==5.1.0                    # PDF
python-docx==1.1.2              # Word

# --- Dev / Test ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.0                   # test client cho FastAPI
